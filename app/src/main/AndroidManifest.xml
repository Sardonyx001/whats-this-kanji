<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WhatsThisKanji">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.WhatsThisKanji">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" />
            </intent-filter>
        </activity>

        <!-- Activity for initializing dictionary -->
        <activity
            android:name=".InitializationActivity"
            android:exported="false"
            android:theme="@style/Theme.WhatsThisKanji" />

        <!-- Activity for reviewing saved words -->
        <activity
            android:name=".SavedWordsActivity"
            android:exported="false"
            android:theme="@style/Theme.WhatsThisKanji" />

        <!-- Router activity for processing selected text -->
        <activity
            android:name=".TextProcessRouter"
            android:exported="true"
            android:label="Kanji Reading"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.PROCESS_TEXT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <!-- Popup window for short text -->
        <activity
            android:name=".PopupTextActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <!-- Bottom sheet for medium text -->
        <activity
            android:name=".BottomSheetTextActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <!-- Full screen activity for long text -->
        <activity
            android:name=".ProcessTextActivity"
            android:exported="false"
            android:theme="@style/Theme.WhatsThisKanji" />

        <!-- Settings activity -->
        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.WhatsThisKanji" />
    </application>

</manifest>